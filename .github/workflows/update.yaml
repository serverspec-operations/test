name: check

on:
  pull_request:
    types:
      - closed

jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v3
      - run: ./update_pr_branches.sh
